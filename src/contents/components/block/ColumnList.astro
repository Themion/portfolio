---
import NotionBlocks from './NotionBlocks.astro';
import type { NotionBlockProps } from '~/contents/types/block';

type Props = NotionBlockProps<'column_list'>;

const { block, components } = Astro.props;
---

<div class="notion-block-column-list">
  {block.has_children && <NotionBlocks blocks={block.children} components={components} />}
</div>
