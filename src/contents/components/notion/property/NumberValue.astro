---
import type { NotionPropertyProps } from './types';

type Props = NotionPropertyProps<'number'>;

const { property } = Astro.props;
---

{property.number !== null && <span class="notion-property-number">{property.number}</span>}
