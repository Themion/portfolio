---
import { RichText } from '../rich-text';
import NotionBlocks from './NotionBlocks.astro';
import type { NotionBlockProps } from './types';

type Props = NotionBlockProps<'callout'>;

const { block, components } = Astro.props;

const color = block.callout.color;
---

<div class="notion-block-callout" data-notion-color={color !== 'default' ? color : undefined}>
  <span>
    <RichText richText={block.callout.rich_text} />
  </span>
  {block.has_children && <NotionBlocks blocks={block.children} components={components} />}
</div>
