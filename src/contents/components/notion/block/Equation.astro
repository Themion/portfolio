---
import Equation from '../rich-text/Equation.astro';
import type { NotionBlockProps } from './types';

type Props = NotionBlockProps<'equation'>;

const { block } = Astro.props;
---

<Equation expression={block.equation.expression} />
