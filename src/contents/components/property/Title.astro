---
import type { NotionPropertyProps } from '~/contents/types';

import { RichText } from '../rich-text';

type Props = NotionPropertyProps<'title'>;

const { property } = Astro.props;
---

<RichText richText={property.title} />
