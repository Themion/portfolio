---
import type { NotionPropertyProps } from '~/contents/types';

type Props = NotionPropertyProps<'checkbox'>;

const { property } = Astro.props;
---

<input type="checkbox" checked={property.checkbox} disabled />
