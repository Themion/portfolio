---
import type { NotionPropertyProps } from '~/contents/types';

type Props = NotionPropertyProps<'url'>;

const { property } = Astro.props;
---

{
  property.url && (
    <a class="notion-property-url" href={property.url}>
      {property.url}
    </a>
  )
}
