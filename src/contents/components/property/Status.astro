---
import type { NotionPropertyProps } from '~/contents/types';

type Props = NotionPropertyProps<'status'>;

const { property } = Astro.props;
---

{
  property.status && (
    <span class="notion-property-status" data-notion-color={property.status.color}>
      {property.status.name}
    </span>
  )
}
