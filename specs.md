# 포트폴리오 — 기능 요구사항

현재 만든 초안(HTML 아티팩트)을 기준으로 정리한 기능 요구사항입니다. Next.js/Astro 등으로 직접 구현할 때 참고용 스펙 문서로 쓰시면 됩니다.

## 1. 전체 구조

- 단일 페이지(SPA 형태)로 구성되며, 섹션은 상단부터 순서대로 **About → Skills → Projects → Education** 4개입니다.
- 각 섹션은 `id`를 가지고 있어 네비게이션에서 해당 섹션으로 스크롤 이동할 수 있어야 합니다.
- 상단에 sticky 네비게이션 바가 있으며, 스크롤해도 항상 화면 상단에 고정됩니다.

## 2. 네비게이션(Nav)

- nav item은 4개 섹션(About / Skills / Projects / Education)에 각각 대응합니다.
- item 클릭 시 해당 섹션으로 **부드럽게(smooth) 애니메이션되며 스크롤 이동**해야 합니다.
- 현재 화면에 보이고 있는 섹션에 대응하는 nav item은 활성 상태로 시각적으로 구분되어야 합니다(스크롤 위치를 감지해 자동으로 갱신).
  - 구현 방식: `IntersectionObserver`로 각 섹션의 뷰포트 진입 여부를 감지해 활성 item을 갱신.
- 네비게이션 영역에는 다크모드 토글 버튼과 PDF 저장 버튼도 함께 위치합니다.

## 3. About 섹션

- 이름, 직함, 자기소개 문단을 표시합니다.
- **Contact 정보는 브라우저 네이티브 기능을 활용**해야 합니다.
  - 이메일: `mailto:` 링크
  - 전화번호: `tel:` 링크
  - GitHub 등 외부 링크: 새 탭에서 열리는 일반 링크(`target="_blank"`, `rel="noopener"`)
- (선택 사항) 나를 소개하는 키워드 태그 — 현재 초안에서는 사용자 요청으로 제거된 상태입니다. 필요 시 다시 추가 가능한 선택 항목으로만 남겨둡니다.

## 4. Skills 섹션

- 스킬은 **Frontend / Backend / 그 외** 3개 그룹으로 분류되어 각 그룹 제목과 함께 표시됩니다.
- 각 스킬은 **아이콘(약어 배지) + 이름**으로 표시되는 카드 형태입니다. 모든 그룹의 카드가 동일한 시각적 스타일을 공유해야 합니다.
- 스킬 카드를 클릭하면 **모달**이 뜨고, 모달에는 다음 내용이 표시됩니다.
  - 아이콘(배지), 스킬 이름
  - "이 스킬로 무엇을 할 수 있는지"를 설명하는 한두 문장의 설명
- 모달은 다음 방식으로 닫을 수 있어야 합니다: 닫기 버튼 클릭, 배경(backdrop) 클릭, `Esc` 키 입력.
- 접근성: 모달이 열리면 포커스가 모달 내부(닫기 버튼)로 이동하고, 닫히면 모달을 열었던 트리거 요소로 포커스가 복귀해야 합니다.

## 5. Projects 섹션

### 5.1 기본 표시 및 폴딩

- 프로젝트는 카드 리스트로 표시되며, 각 카드는 기본적으로 **펼쳐진(open) 상태**로 시작합니다.
- 카드 헤더(제목, 기간, 역할)를 클릭하면 카드 본문 전체가 접히거나 펼쳐집니다.
- 카드 본문에는 기술 스택, 배경, 내가 한 일(목록), 기술적 의사결정, 결과가 포함됩니다.

### 5.2 기술 스택 표시

- 프로젝트별 기술 스택은 **텍스트 라벨 없이 아이콘(약어 배지)만** 표시합니다.
- 각 배지에는 `title` 속성(또는 이에 준하는 툴팁)으로 전체 기술명을 노출해, 마우스 오버 시 이름을 확인할 수 있어야 합니다.
- Skills 섹션의 배지와 시각적 스타일이 일관되어야 합니다.

### 5.3 기술적 의사결정

- "기술적 의사결정"은 프로젝트 카드 하위의 별도 블록입니다.
- 이 블록은 **섹션 전체가 아니라, 각 의사결정 항목 하나하나가 개별적으로 접고 펼쳐지는 구조**입니다.
  - 각 항목은 한 줄(제목만)로 표시된 상태가 기본이며, **모든 항목은 기본 접힘(collapsed) 상태**로 시작합니다.
  - 항목의 제목 줄을 클릭하면 그 항목의 상세 설명만 펼쳐지고, 다른 항목에는 영향을 주지 않습니다(독립적인 토글).

### 5.4 핵심 프로젝트만 보기

- 프로젝트에는 "핵심 프로젝트" 여부(core / secondary) 속성이 있습니다.
- Projects 섹션 상단에 **"핵심 프로젝트만 보기" 토글 스위치**가 있으며, 기본값은 **ON**입니다.
  - ON 상태: `secondary`로 표시된, 덜 중요한 프로젝트는 화면에서 숨겨집니다.
  - OFF 상태: 모든 프로젝트가 표시됩니다.
- 토글은 마우스 클릭과 키보드(Enter/Space)로 모두 조작 가능해야 합니다(접근성).

## 6. Education 섹션

- 학력 정보와 경력 타임라인을 표시합니다.
- 카드 형태로 구성되며, 다른 섹션의 카드/그리드와 시각적 너비 및 톤이 일관되어야 합니다(현재 초안은 컨테이너 전체 너비를 사용).

## 7. 다크모드

- 페이지 최초 진입 시 다크모드 상태는 **사용자 브라우저(OS)의 시스템 설정**(`prefers-color-scheme`)을 따라야 합니다.
- 네비게이션에 다크모드 토글 버튼이 있어, 사용자가 수동으로 라이트/다크를 전환할 수 있습니다.
- 수동 전환 값은 시스템 설정보다 우선 적용됩니다(사용자가 직접 고른 값이 있으면 그 값을 따름).
- 다크모드 여부에 따라 배경, 텍스트, 테두리, 강조색 등 전체 색상 토큰이 일관되게 전환되어야 합니다.

## 8. PDF 저장(출력) 기능

- 네비게이션에 "PDF 저장" 버튼이 있어, 클릭 시 현재 페이지 내용을 PDF로 저장/출력할 수 있어야 합니다.
- 출력 시에는 다음이 자동으로 적용되어야 합니다.
  - 네비게이션 바, 다크모드 토글, PDF 버튼, 토글 스위치, 화살표 아이콘 등 인터랙션 전용 UI 요소는 출력물에서 숨김
  - 접혀 있던 프로젝트 카드와 기술적 의사결정 항목은 **모두 펼쳐진 상태로 강제 출력**
  - "핵심 프로젝트만 보기" 필터는 해제되어 **모든 프로젝트가 출력**
  - 출력이 끝나면 화면(UI) 상태는 사용자가 보던 상태로 원상 복구되어야 함(펼침/접힘, 필터 상태 등)

> 참고: 현재 초안은 브라우저 인쇄 기능(`window.print()`)을 활용한 방식으로 구현되어 있습니다. 실제 요구사항에 명시된 `react-pdf`(또는 `@react-pdf/renderer`) 기반 구현은 번들러가 있는 실제 프로젝트에서만 안정적으로 동작하는 라이브러리라 정적 HTML 아티팩트에서는 넣지 못했습니다. 직접 구현하실 때는 이 부분을 react-pdf 기반 컴포넌트로 교체하시면 됩니다.

## 9. 콘텐츠 데이터 모델 (참고)

실제 구현 시 아래와 같은 데이터 구조로 콘텐츠를 분리해 관리하는 것을 권장합니다(현재 초안은 이 구조를 그대로 JS 객체로 하드코딩한 상태).

- `contacts`: `{ label, href, icon }[]`
- `skillGroups`: `{ key, label, items: { tag, name, desc }[] }[]`
- `projects`: `{ id, core, title, period, role, tech: string[], background, contributions: string[], decisions: { t, d }[], result }[]`
- `timeline`: `{ period, text }[]`
- `techIcon`: 기술명 → 아이콘 약어 매핑 테이블(Skills와 Projects 양쪽에서 공유)

## 10. 비기능 요구사항

- `prefers-reduced-motion`을 존중해 스크롤/애니메이션을 최소화할 수 있어야 합니다.
- 폴딩 애니메이션, 모달, 토글 등 모든 인터랙션 요소는 키보드로도 접근·조작 가능해야 합니다.
- 반응형: 좁은 화면(모바일)에서는 네비게이션 item이 축소되거나 숨겨질 수 있습니다(현재 초안은 640px 이하에서 nav item을 숨김 — 실제 구현 시 햄버거 메뉴 등으로 대체 검토 필요).
